#include <stdio.h>
/**
 * main - prints the exact value of b but not for c
 * Return: Always 0 .
 **/
int main(void)
{
	int b = 10, c = -10;

	printf("Value of b is %u\n", b);
	printf("Value of c is %u\n", c);

	return (0);
}
